# TC-011

| Field | Description |
|---|---|
| **Test ID** | TC-011 |
| **Feature** | Wellness Goals |
| **Scenario** | CRUD — Create a goal entry with valid data |
| **Steps** | 1. Log in with a valid account. 2. Navigate to the Wellness Goals page. 3. Enter a goal title, description, and target date. 4. Submit the goal form. |
| **Expected Result** | A new goal is saved to the `wellness_goals` table with the correct `user_id`, `title`, `description`, `target_date`, and `status` set to `active`. |
| **Actual Result** | Goal was successfully created and saved to the database with all correct fields and status defaulting to `active`. |
| **Status** | Pass |
| **Notes** | Confirm `abandoned` and `completed` status updates in a follow-up test next sprint. |
