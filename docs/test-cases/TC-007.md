# TC-007

| Field | Description |
|---|---|
| **Test ID** | TC-007 |
| **Feature** | Journal / Reflection |
| **Scenario** | CRUD — Create a journal entry with tags |
| **Steps** | 1. Log in with a valid account. 2. Navigate to the Journal / Reflection page. 3. Enter text content in the entry field. 4. Add at least one tag using the tag input field. 5. Submit the journal entry. |
| **Expected Result** | A new entry is saved to the `journal_entries` table with the correct `user_id`, `content`, `tags[]`, and `created_at`. |
| **Actual Result** | Entry was successfully created and saved to the database with all correct fields including tags array. |
| **Status** | Pass |
| **Notes** | Tags stored correctly as array in `tags[]` column. Cross-check tags against km-architecture.md taxonomy when available. |
