# TC-006

| Field | Description |
|---|---|
| **Test ID** | TC-006 |
| **Feature** | Mood Tracker |
| **Scenario** | CRUD — Create a mood log entry with valid data |
| **Steps** | 1. Log in with a valid account. 2. Navigate to the Mood Tracker page. 3. Select a mood score between 1 and 10 using the slider or emoji picker. 4. Enter an optional note in the note field. 5. Submit the mood log form. |
| **Expected Result** | A new entry is saved to the `mood_logs` table with the correct `user_id`, `mood_score`, `note`, and `created_at`. |
| **Actual Result** | Entry was successfully created and saved to the database with all correct fields. |
| **Status** | Pass |
| **Notes** | RLS policy confirmed — `auth.uid() = user_id`. Tested with and without a note. |
